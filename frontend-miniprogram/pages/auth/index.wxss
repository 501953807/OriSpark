/* pages/auth/index.wxss */
.auth-page { display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: 100vh; }
.auth-logo { font-size: 48rpx; font-weight: bold; color: #1a237e; margin-bottom: 16rpx; }
.auth-title { font-size: 34rpx; color: #333; margin-bottom: 60rpx; }
.auth-hint { font-size: 24rpx; color: #999; margin-top: 24rpx; }
